import React from 'react';

const ponmejis = (props) => (
    <div className="input-field inline">
        <a className="btn-floating waves-effect waves-light blue" onClick={props.dameresultado}><i className="material-icons">add</i></a>
    </div>
);

export default ponmejis;