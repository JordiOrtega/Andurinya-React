import React from 'react';

const restamejis = (props) => (
    <div className="input-field inline">
        <a className="btn-floating waves-effect waves-light blue" onClick={props.dameresultado}><i className="material-icons">indeterminate_check_box</i></a>
    </div>
);

export default restamejis;